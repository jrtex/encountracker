# Local Test Environment Overrides
# Copy this file to .env.test.local and fill in your PostgreSQL admin password
# .env.test.local is NOT committed to git (for security)

# PostgreSQL Admin Password (for creating test database)
POSTGRES_ADMIN_PASSWORD=your_postgres_password_here
